/* ProductManagement Index — ui-datatable handles data loading; this script is a no-op placeholder.
   The ui-datatable component fetches from GetList and renders rows automatically via column config + route-template. */
